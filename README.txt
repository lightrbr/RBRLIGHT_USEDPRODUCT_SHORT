Used catalog (same layout as New catalog)

Upload to a NEW repo root:
- index.html
- style.css
- app.js
- data.json

This version shows a clear USED label (header + cards + product sheet).
Photos are not included (photo field empty) -> initials placeholder will be shown.
If later you add photos, put them in assets/ and set photo: "assets/yourfile.jpg" in data.json.
